Next.js migration of the La Malle en Coin static site.

To run locally:

1. Install dependencies: npm install
2. Start dev server: npm run dev

Notes:
- Assets moved to /public/assets
- Styles in /styles/globals.css
- Components in /components
- Basic pages created under /pages
- Update logo at /public/assets/img/logo/logo.png
